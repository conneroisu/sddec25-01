[project]
name = "sddec25-01"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.3.5",
]

[tool.uv.workspace]
members = ["packages/*", "apps/*"]
